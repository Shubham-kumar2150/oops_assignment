#include <iostream>
using namespace std;
int main() {
    int a, b;
    int result;
    cout << "Enter values of a and b: ";
    cin >> a >> b;
    result = (a + b) * (a + b);
    cout << "Result of (a + b)^2 = " << result << endl;
    return 0;
}
